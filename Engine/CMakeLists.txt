include(FetchContent)

FetchContent_Declare(
    glfw
    GIT_REPOSITORY https://github.com/glfw/glfw.git
    GIT_TAG 3.4
)
FetchContent_MakeAvailable(glfw)

FetchContent_Declare(
    glm
    GIT_REPOSITORY https://github.com/g-truc/glm.git
    GIT_TAG 1.0.1
)
FetchContent_MakeAvailable(glm)

FetchContent_Declare(
    entt
    GIT_REPOSITORY https://github.com/skypjack/entt.git
    GIT_TAG v3.15.0
)
FetchContent_MakeAvailable(entt)

FetchContent_Declare(
    spdlog
    GIT_REPOSITORY https://github.com/gabime/spdlog.git
    GIT_TAG v1.15.3
)
FetchContent_MakeAvailable(spdlog)

FetchContent_Declare(
    glad
    GIT_REPOSITORY https://github.com/Dav1dde/glad.git
    GIT_TAG glad2
    SOURCE_SUBDIR cmake
)
FetchContent_MakeAvailable(glad)
glad_add_library(glad_gl STATIC REPRODUCIBLE LOADER API gl:core=4.6)

FetchContent_Declare(
    imgui
    GIT_REPOSITORY https://github.com/ocornut/imgui.git
    GIT_TAG docking
)
FetchContent_GetProperties(imgui)
if(NOT imgui_POPULATED)
    FetchContent_Populate(imgui)
endif()

add_library(imgui STATIC
    ${imgui_SOURCE_DIR}/imgui.cpp
    ${imgui_SOURCE_DIR}/imgui_demo.cpp
    ${imgui_SOURCE_DIR}/imgui_draw.cpp
    ${imgui_SOURCE_DIR}/imgui_tables.cpp
    ${imgui_SOURCE_DIR}/imgui_widgets.cpp
    ${imgui_SOURCE_DIR}/backends/imgui_impl_glfw.cpp
    ${imgui_SOURCE_DIR}/backends/imgui_impl_opengl3.cpp
)

target_include_directories(imgui PUBLIC
    ${imgui_SOURCE_DIR}
    ${imgui_SOURCE_DIR}/backends
)

# imgui_impl_glfw.cpp needs GLFW headers
target_link_libraries(imgui PUBLIC glfw)

FetchContent_Declare(
    imguizmo
    GIT_REPOSITORY https://github.com/CedricGuillemet/ImGuizmo.git
    GIT_TAG master
)
FetchContent_GetProperties(imguizmo)
if(NOT imguizmo_POPULATED)
    FetchContent_Populate(imguizmo)
endif()

add_library(imguizmo STATIC
    ${imguizmo_SOURCE_DIR}/src/ImGuizmo.cpp
)
target_include_directories(imguizmo PUBLIC ${imguizmo_SOURCE_DIR}/src)
target_link_libraries(imguizmo PUBLIC imgui)

set(ASSIMP_BUILD_ALL_IMPORTERS_BY_DEFAULT OFF CACHE BOOL "" FORCE)
set(ASSIMP_BUILD_OBJ_IMPORTER  ON  CACHE BOOL "" FORCE)
set(ASSIMP_BUILD_FBX_IMPORTER  ON  CACHE BOOL "" FORCE)
set(ASSIMP_BUILD_ALL_EXPORTERS_BY_DEFAULT OFF CACHE BOOL "" FORCE)
set(ASSIMP_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(ASSIMP_INSTALL    OFF CACHE BOOL "" FORCE)
set(ASSIMP_NO_EXPORT  ON  CACHE BOOL "" FORCE)

FetchContent_Declare(
    assimp
    GIT_REPOSITORY https://github.com/assimp/assimp.git
    GIT_TAG v5.4.3
    GIT_SHALLOW TRUE
)
FetchContent_MakeAvailable(assimp)

FetchContent_Declare(
    nlohmann_json
    GIT_REPOSITORY https://github.com/nlohmann/json.git
    GIT_TAG v3.11.3
    GIT_SHALLOW TRUE
)
FetchContent_MakeAvailable(nlohmann_json)

FetchContent_Declare(
    miniz
    GIT_REPOSITORY https://github.com/richgel999/miniz.git
    GIT_TAG 3.0.2
)
FetchContent_GetProperties(miniz)
if(NOT miniz_POPULATED)
    FetchContent_Populate(miniz)
endif()

# miniz_export.h is generated by miniz's own CMake; stub it out for static linking
file(WRITE ${miniz_SOURCE_DIR}/miniz_export.h
"#pragma once\n#define MINIZ_EXPORT\n#define MINIZ_NO_EXPORT\n")

add_library(miniz STATIC
    ${miniz_SOURCE_DIR}/miniz.c
    ${miniz_SOURCE_DIR}/miniz_tdef.c
    ${miniz_SOURCE_DIR}/miniz_tinfl.c
    ${miniz_SOURCE_DIR}/miniz_zip.c
)
target_include_directories(miniz PUBLIC ${miniz_SOURCE_DIR})

# Disable everything we don't need before Jolt is populated
set(PHYSICS_DOUBLE_PRECISION        OFF CACHE BOOL "" FORCE)
set(TARGET_UNIT_TESTS               OFF CACHE BOOL "" FORCE)
set(TARGET_SAMPLES                  OFF CACHE BOOL "" FORCE)
set(TARGET_VIEWER                   OFF CACHE BOOL "" FORCE)
set(TARGET_PERFORMANCE_TEST         OFF CACHE BOOL "" FORCE)
set(TARGET_HELLO_WORLD              OFF CACHE BOOL "" FORCE)
# Match the default CMake CRT linkage (/MD) on MSVC instead of Jolt's /MT default
set(USE_STATIC_MSVC_RUNTIME_LIBRARY OFF CACHE BOOL "" FORCE)

FetchContent_Declare(
    JoltPhysics
    GIT_REPOSITORY https://github.com/jrouwe/JoltPhysics.git
    GIT_TAG        v5.2.0
    GIT_SHALLOW    TRUE
    SOURCE_SUBDIR  Build   # CMakeLists.txt lives in Build/, not the repo root
)
FetchContent_MakeAvailable(JoltPhysics)

# Jolt's Build/CMakeLists.txt enables /WX + /Zc:enumTypes by default, which
# causes Windows SDK 10.0.26100 headers to emit C4865 warnings that are then
# promoted to errors.  Strip /WX from the Jolt target so our SDK headers don't
# break the Jolt build.
if(MSVC AND TARGET Jolt)
    target_compile_options(Jolt PRIVATE /WX- /wd4865)
endif()

# miniaudio — single-header audio backend. Populated only (no CMakeLists we want);
# its implementation is compiled into MyEngine via src/Audio/miniaudio_impl.c, and the
# include dir is added PRIVATE so the dependency never leaks into Engine's public API.
FetchContent_Declare(
    miniaudio
    GIT_REPOSITORY https://github.com/mackron/miniaudio.git
    GIT_TAG        0.11.25
)
FetchContent_GetProperties(miniaudio)
if(NOT miniaudio_POPULATED)
    FetchContent_Populate(miniaudio)
endif()

# Tracy client — compiled in only with -DDIAMOND_TRACY=ON (root CMakeLists).
# TRACY_ON_DEMAND defers all collection until the Tracy GUI connects, so an
# enabled build costs ~nothing while unattached. The GUI must be the same
# version as this tag — the wire protocol is version-locked.
if(DIAMOND_TRACY)
    set(TRACY_ENABLE    ON CACHE BOOL "" FORCE)
    set(TRACY_ON_DEMAND ON CACHE BOOL "" FORCE)
    FetchContent_Declare(
        tracy
        GIT_REPOSITORY https://github.com/wolfpld/tracy.git
        GIT_TAG        v0.13.1
        GIT_SHALLOW    TRUE
    )
    FetchContent_MakeAvailable(tracy)
endif()

find_package(OpenGL REQUIRED)

# ── Vulkan backend dependencies ──────────────────────────────────────────────
# Fetched (not the system loader): we load entry points via volk with
# VK_NO_PROTOTYPES, so MyEngine never links the Vulkan loader directly. The
# installed Vulkan SDK still supplies validation layers + glslangValidator at
# dev time. See Docs/vulkan-renderer-design.md.
if(DIAMOND_ENABLE_VULKAN)
    FetchContent_Declare(
        vulkan_headers
        GIT_REPOSITORY https://github.com/KhronosGroup/Vulkan-Headers.git
        GIT_TAG        v1.4.350
        GIT_SHALLOW    TRUE
    )
    FetchContent_MakeAvailable(vulkan_headers)

    # Point volk's header search at the fetched Vulkan-Headers (its CMake checks
    # VULKAN_HEADERS_INSTALL_DIR first). Forces use of our pinned 1.4.350 headers
    # rather than whatever the installed SDK happens to be. ON+FORCE also clears
    # any stale VOLK_PULL_IN_VULKAN cache value.
    set(VOLK_PULL_IN_VULKAN ON CACHE BOOL "" FORCE)
    set(VULKAN_HEADERS_INSTALL_DIR "${vulkan_headers_SOURCE_DIR}" CACHE PATH "" FORCE)
    FetchContent_Declare(
        volk
        GIT_REPOSITORY https://github.com/zeux/volk.git
        GIT_TAG        vulkan-sdk-1.4.350.0
        GIT_SHALLOW    TRUE
    )
    FetchContent_MakeAvailable(volk)

    # VMA is header-only; its single implementation TU is compiled into MyEngine
    # via src/Platform/Vulkan/vma_impl.cpp (mirrors the miniaudio_impl.c pattern).
    FetchContent_Declare(
        vma
        GIT_REPOSITORY https://github.com/GPUOpen-LibrariesAndSDKs/VulkanMemoryAllocator.git
        GIT_TAG        v3.4.0
        GIT_SHALLOW    TRUE
    )
    FetchContent_MakeAvailable(vma)
endif()

add_library(MyEngine STATIC
    src/DebugDraw.cpp
    src/Core/Input.cpp
    src/Application.cpp
    src/Assets/ImageLoader.cpp
    src/Assets/DDSLoader.cpp
    src/Assets/ModelImporter.cpp
    src/Assets/GltfImporter.cpp
    src/Assets/cgltf_impl.c
    src/Audio/miniaudio_impl.c
    src/Audio/AudioEngine.cpp
    src/Animation/AnimationSampler.cpp
    src/Animation/AnimationSystem.cpp
    src/Animation/AnimatorAPI.cpp
    src/Animation/IKSolver.cpp
    src/Animation/IKSystem.cpp
    src/Platform/OpenGL/GLRendererStats.cpp
    src/Profiling/CPUProfiler.cpp
    src/Platform/OpenGL/Resources/OpenGLMesh.cpp
    src/Platform/OpenGL/Resources/OpenGLParticleRenderer.cpp
    src/Platform/OpenGL/Resources/OpenGLRenderer2D.cpp
    src/Platform/OpenGL/Resources/OpenGLRendererAPI.cpp
    src/Platform/OpenGL/Resources/OpenGLShader.cpp
    src/Platform/OpenGL/Resources/OpenGLTexture.cpp
    src/Platform/OpenGL/Passes/IBL/OpenGLIBLPass.cpp
    src/Platform/OpenGL/Passes/Shadows/OpenGLShadowPass.cpp
    src/Platform/OpenGL/Passes/Shadows/OpenGLCSMPass.cpp
    src/Platform/OpenGL/Passes/Deferred/OpenGLGBufferPass.cpp
    src/Platform/OpenGL/Passes/Deferred/OpenGLSSAOPass.cpp
    src/Platform/OpenGL/Passes/Deferred/OpenGLDeferredLightingPass.cpp
    src/Platform/OpenGL/Passes/Forward/OpenGLPBRSurfacePass.cpp
    src/Platform/OpenGL/Passes/Forward/OpenGLTransparencyPass.cpp
    src/Platform/OpenGL/Passes/PostProcess/OpenGLTonemapPass.cpp
    src/Platform/OpenGL/Passes/PostProcess/OpenGLBloomPass.cpp
    src/Platform/OpenGL/Passes/PostProcess/OpenGLFXAAPass.cpp
    src/Renderer/ParticleRenderer.cpp
    src/Renderer/RenderGraph.cpp
    src/Renderer/Font.cpp
    src/Renderer/Material.cpp
    src/Renderer/MeshData.cpp
    src/Renderer/Renderer2D.cpp
    src/Renderer/RendererAPI.cpp
    src/Renderer/SceneRenderer.cpp
    src/Renderer/Shader.cpp
    src/Renderer/TextureData.cpp
    src/Scene/Scene.cpp
    src/Scene/SceneSerializer.cpp
    src/Scene/SceneSystem.cpp
    src/Scene/DebugVisualization.cpp
    src/Scene/AudioSystem.cpp
    src/Scene/ParticleSystem.cpp
    src/Scene/TransformSystem.cpp
    src/Scene/UISystem.cpp
    src/Scene/UIRenderSystem.cpp
    src/Scene/UIInputSystem.cpp
    src/Scene/UINavigationSystem.cpp
    src/Scene/UUIDGenerator.cpp
    src/Scene/Physics/PhysicsSystem.cpp
    src/Scene/Physics/RagdollConfig.cpp
    src/Renderer/RHI/RHIDevice.cpp
    src/Renderer/RHI/RHIRenderGraph.cpp
    # Compiled in every build (like SceneRenderer.cpp): collapses to a stub
    # without DIAMOND_ENABLE_VULKAN so Sandbox's --vulkan dispatch always links.
    src/Platform/Vulkan/VulkanEditorBackend.cpp
)

target_include_directories(MyEngine
    PUBLIC
        Include
        ${CMAKE_SOURCE_DIR}/ThirdParty/stb
    PRIVATE
        src
        ${CMAKE_SOURCE_DIR}/ThirdParty/cgltf
        ${joltphysics_SOURCE_DIR}
        ${miniaudio_SOURCE_DIR}
)

target_link_libraries(MyEngine
    PUBLIC
        glfw
        OpenGL::GL
        glm::glm
        EnTT::EnTT
        spdlog::spdlog
        imgui
        glad_gl
        assimp::assimp
        # Public: scene/prefab/asset serialization (Scene/SceneSerializer.h,
        # Assets/*Asset.h, Scene/ComponentRegistry.h) exposes nlohmann types in
        # engine headers consumed by the editor and (M7) the runtime.
        nlohmann_json::nlohmann_json
    PRIVATE
        Jolt
)

# AssetPathUtils.h anchors portable "Assets/..." paths at the compile-time asset
# root (same value Sandbox defines for its own TUs; ODR-safe because identical).
# The M7 packager will replace this with an exe-relative root resolved at runtime.
target_compile_definitions(MyEngine PRIVATE
    ASSETS_DIR="${CMAKE_SOURCE_DIR}/Assets"
)

# PUBLIC on both: the DIAMOND_PROFILE_* macros live in the public CPUProfiler.h
# and expand to Tracy zones in every TU that uses them (Sandbox included), so
# downstream targets need the include path, the define, and the client symbols.
if(DIAMOND_TRACY)
    target_link_libraries(MyEngine PUBLIC Tracy::TracyClient)
    target_compile_definitions(MyEngine PUBLIC DIAMOND_TRACY)
endif()

# ── Vulkan backend wiring ────────────────────────────────────────────────────
# volk + VMA are PRIVATE: Vulkan types never appear in MyEngine's public headers
# (same discipline as Jolt). VK_NO_PROTOTYPES forces all calls through volk's
# loaded pointers. DIAMOND_ENABLE_VULKAN gates the backend at compile time.
if(DIAMOND_ENABLE_VULKAN)
    target_sources(MyEngine PRIVATE
        src/Platform/Vulkan/vma_impl.cpp
        src/Platform/Vulkan/VulkanContext.cpp
        src/Platform/Vulkan/VulkanSwapchain.cpp
        src/Platform/Vulkan/VulkanBuffer.cpp
        src/Platform/Vulkan/VulkanImage.cpp
        src/Platform/Vulkan/RHI/VulkanRHIDevice.cpp
        src/Platform/Vulkan/RHI/VulkanRHIResources.cpp
        src/Platform/Vulkan/Passes/PostProcess/VulkanTonemapPass.cpp
        src/Platform/Vulkan/Passes/PostProcess/VulkanTAAPass.cpp
        src/Platform/Vulkan/Passes/PostProcess/VulkanFXAAPass.cpp
        src/Platform/Vulkan/Passes/PostProcess/VulkanBloomPass.cpp
        src/Platform/Vulkan/Passes/Deferred/VulkanGBufferPass.cpp
        src/Platform/Vulkan/Passes/Deferred/VulkanSSAOPass.cpp
        src/Platform/Vulkan/Passes/Deferred/VulkanSSRPass.cpp
        src/Platform/Vulkan/Passes/Deferred/VulkanDeferredLightingPass.cpp
        src/Platform/Vulkan/Passes/Deferred/VulkanSkyboxPass.cpp
        src/Platform/Vulkan/Passes/Debug/VulkanDebugDrawPass.cpp
        src/Platform/Vulkan/Passes/Forward/VulkanPBRSurfacePass.cpp
        src/Platform/Vulkan/Passes/Forward/VulkanTransparencyPass.cpp
        src/Platform/Vulkan/Passes/Shadows/VulkanCSMPass.cpp
        src/Platform/Vulkan/Passes/Shadows/VulkanSpotShadowPass.cpp
        src/Platform/Vulkan/Passes/Shadows/VulkanPointShadowPass.cpp
        src/Platform/Vulkan/Passes/IBL/VulkanIBLPass.cpp
        src/Platform/Vulkan/Resources/VulkanParticleRenderer.cpp
        src/Platform/Vulkan/Resources/VulkanRenderer2D.cpp
        src/Platform/Vulkan/Resources/VulkanTexture2D.cpp
        src/Platform/Vulkan/Resources/VulkanThumbnailRenderer.cpp
        src/Platform/Vulkan/VulkanImGuiLayer.cpp
        src/Platform/Vulkan/VulkanMeshDemo.cpp
        src/Platform/Vulkan/VulkanSceneDemo.cpp
        # ImGui's Vulkan renderer backend. Compiled into MyEngine (not the imgui
        # static lib) so it inherits VK_NO_PROTOTYPES + the volk include path; USE_VOLK
        # makes it call volk's already-loaded function pointers (no LoadFunctions dance).
        ${imgui_SOURCE_DIR}/backends/imgui_impl_vulkan.cpp
    )
    target_link_libraries(MyEngine PRIVATE
        volk
        VulkanMemoryAllocator
    )

    # ── Compile the Vulkan backend's GLSL → SPIR-V at build time ─────────────
    # glslangValidator ships with the Vulkan SDK. Offline compilation keeps the
    # dependency footprint light (no shaderc/glslang FetchContent); runtime
    # libshaderc for hot-reload arrives with the material/shader-library port. The
    # output dir is baked in as DIAMOND_VULKAN_SHADER_DIR so the loader needs no
    # working-dir assumptions.
    find_program(GLSLANG_VALIDATOR NAMES glslangValidator
        HINTS $ENV{VULKAN_SDK}/Bin $ENV{VULKAN_SDK}/bin)
    if(NOT GLSLANG_VALIDATOR)
        message(FATAL_ERROR
            "glslangValidator not found — install the Vulkan SDK (or set VULKAN_SDK). "
            "Required to compile the Vulkan backend's shaders.")
    endif()

    set(_vk_shader_src ${CMAKE_SOURCE_DIR}/Assets/Shaders/Vulkan)
    set(_vk_spv_dir    ${CMAKE_BINARY_DIR}/shaders)
    file(MAKE_DIRECTORY ${_vk_spv_dir})
    set(_vk_spv_outputs)
    foreach(_shader mesh.vert mesh.frag fullscreen.vert tonemap.frag fxaa.frag
                    bloom_extract.frag bloom_blur.frag bloom_composite.frag
                    gbuffer.vert gbuffer.frag gbuffer_debug.frag
                    gbuffer_skinned.vert
                    ssao.frag ssao_blur.frag ssr.frag ssr_composite.frag
                    taa_resolve.frag
                    csm_depth.vert csm_depth.frag csm_depth_skinned.vert
                    spot_depth_copy.frag
                    point_shadow.vert point_shadow.frag point_shadow_skinned.vert
                    deferred_lighting.frag
                    cubemap.vert equirect_to_cubemap.frag
                    irradiance_convolution.frag prefilter.frag brdf_lut.frag
                    copy.frag skybox.vert skybox.frag
                    pbr_surface.vert pbr_surface.frag
                    transparent.vert transparent.frag
                    debugdraw.vert debugdraw.frag
                    renderer2d.vert renderer2d.frag
                    particle.vert particle.frag
                    thumbnail.vert thumbnail.frag
                    material_preview.vert material_preview.frag)
        add_custom_command(
            OUTPUT  ${_vk_spv_dir}/${_shader}.spv
            COMMAND ${GLSLANG_VALIDATOR} -V ${_vk_shader_src}/${_shader} -o ${_vk_spv_dir}/${_shader}.spv
            DEPENDS ${_vk_shader_src}/${_shader}
            COMMENT "Compiling Vulkan shader ${_shader} → SPIR-V"
            VERBATIM)
        list(APPEND _vk_spv_outputs ${_vk_spv_dir}/${_shader}.spv)
    endforeach()
    add_custom_target(VulkanShaders DEPENDS ${_vk_spv_outputs})
    add_dependencies(MyEngine VulkanShaders)

    target_compile_definitions(MyEngine PRIVATE
        DIAMOND_ENABLE_VULKAN
        VK_NO_PROTOTYPES
        IMGUI_IMPL_VULKAN_USE_VOLK   # imgui_impl_vulkan calls volk's loaded pointers
        DIAMOND_VULKAN_SHADER_DIR="${_vk_spv_dir}"
        DIAMOND_ASSETS_DIR="${CMAKE_SOURCE_DIR}/Assets"
        # Same glslangValidator this file's offline compile uses — the editor's
        # runtime hot-reload shells out to it again on a source-file change
        # instead of linking a second (shaderc) copy of the same compiler.
        DIAMOND_GLSLANG_VALIDATOR="${GLSLANG_VALIDATOR}"
    )
endif()

# miniaudio backend platform libs (it dynamically loads the device APIs at runtime).
# Windows needs nothing extra; macOS needs the CoreAudio frameworks; Linux needs
# pthread/m/dl for threading and dlopen of ALSA/PulseAudio.
if(APPLE)
    target_link_libraries(MyEngine PRIVATE
        "-framework CoreFoundation" "-framework CoreAudio" "-framework AudioToolbox")
elseif(UNIX)
    find_package(Threads REQUIRED)
    target_link_libraries(MyEngine PRIVATE Threads::Threads ${CMAKE_DL_LIBS} m)
endif()
